# Voicebox Colab — runtime dependencies that are safe on Google Colab.
# Engine-specific packages (qwen-tts, chatterbox-tts, kokoro, hume-tada, LuxTTS)
# are installed in dedicated notebook cells so a missing optional engine
# does not break the studio.

gradio>=4.44.0
soundfile>=0.12.0
librosa>=0.10.0
numpy>=1.24.0
pedalboard>=0.9.0
huggingface_hub>=0.20.0
safetensors>=0.4.0
einops>=0.7.0
tqdm>=4.66.0
python-multipart>=0.0.6
